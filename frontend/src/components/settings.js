import "./settings.css"; // optional shared styling

function Settings() {
  return (
    <div className="settings">
      <div className="settings-panel">
        Settings panel
      </div>
    </div>
  );
}

export default Settings;